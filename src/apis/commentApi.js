import axios from "./axiosClient";

const commentApi = {
    // fetchNewsComments: (id) => axios.get(`comments/${id}/`),
    // createNewsComments: (comment) => axios.post(`/comments/`, comment),
};
export default commentApi;